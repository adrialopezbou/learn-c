#include <stdio.h>
// Constants handled by the preprocessor. These are not variables
#define LOWER 0
#define GREETING "Greetings!"

int main() {
    // Constant variables handled by the compiler
    const int UPPER = 300;
}